# 1. Implementing Caching - lets try redis at the backend first

Install Redis Client
Inside your backend folder (where your Express server is located), install the official Node.js Redis client:

```bash
npm install redis
```
